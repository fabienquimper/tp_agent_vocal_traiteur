[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    slow: tests nécessitant des appels LLM réels (désactivés par défaut en CI)
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestDeprecationWarning
